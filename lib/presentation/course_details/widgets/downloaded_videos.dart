import 'package:flutter/widgets.dart';

class DownloadedVideosWidget extends StatelessWidget {
  const DownloadedVideosWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
