import 'package:flutter/material.dart';

class LessionWidget extends StatelessWidget {
  const LessionWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
